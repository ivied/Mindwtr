name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  # Core package tests
  core:
    runs-on: ubuntu-latest
    name: Core Package
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: ".bun-version"

      - name: Cache Bun dependencies
        uses: actions/cache@9255dc7a253b0ccc959486e2bca901246202afeb # v5.0.1
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: |
          set -e
          bun install || (echo "bun install failed, clearing cache and retrying..." && (bun pm cache rm || bun pm cache clean || true) && bun install)

      - name: TypeScript check
        run: cd packages/core && bun run tsc --noEmit

      - name: Run unit tests
        run: bun run --filter @mindwtr/core test -- --coverage

      - name: Core coverage summary
        if: always()
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = 'packages/core/coverage/coverage-summary.json';
          if (!fs.existsSync(path)) {
            console.log(`No coverage summary found at ${path}`);
            process.exit(0);
          }
          const total = JSON.parse(fs.readFileSync(path, 'utf8')).total;
          const lines = [
            '### Core Coverage',
            '',
            '| Metric | Percent | Covered / Total |',
            '| --- | ---: | ---: |',
            `| Lines | ${total.lines.pct}% | ${total.lines.covered} / ${total.lines.total} |`,
            `| Statements | ${total.statements.pct}% | ${total.statements.covered} / ${total.statements.total} |`,
            `| Functions | ${total.functions.pct}% | ${total.functions.covered} / ${total.functions.total} |`,
            `| Branches | ${total.branches.pct}% | ${total.branches.covered} / ${total.branches.total} |`,
            '',
          ];
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, `${lines.join('\n')}\n`);
          NODE

      - name: Upload core coverage artifact
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: core-coverage
          path: packages/core/coverage
          if-no-files-found: ignore

      - name: Cloud app tests
        run: bun run --filter mindwtr-cloud test

  # Desktop app build and tests (Tauri)
  desktop:
    runs-on: ubuntu-latest
    name: Desktop App (Tauri)
    timeout-minutes: 45
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Install system dependencies (Tauri)
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libasound2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable

      - name: Ensure Rust toolchain (retry)
        shell: bash
        run: |
          for attempt in 1 2 3; do
            if rustup toolchain install stable --profile minimal --no-self-update && rustup default stable; then
              exit 0
            fi
            if [ "$attempt" -eq 3 ]; then
              echo "Rust toolchain setup failed after $attempt attempts" >&2
              exit 1
            fi
            echo "Rust toolchain setup failed on attempt $attempt, retrying..." >&2
            sleep 10
          done

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2.0"

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: ".bun-version"

      - name: Cache Bun dependencies
        uses: actions/cache@9255dc7a253b0ccc959486e2bca901246202afeb # v5.0.1
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: TypeScript check
        run: cd apps/desktop && bun run tsc --noEmit

      - name: Run tests
        run: bun run --filter mindwtr test -- --coverage

      - name: Desktop coverage summary
        if: always()
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = 'apps/desktop/coverage/coverage-summary.json';
          if (!fs.existsSync(path)) {
            console.log(`No coverage summary found at ${path}`);
            process.exit(0);
          }
          const total = JSON.parse(fs.readFileSync(path, 'utf8')).total;
          const lines = [
            '### Desktop Coverage',
            '',
            '| Metric | Percent | Covered / Total |',
            '| --- | ---: | ---: |',
            `| Lines | ${total.lines.pct}% | ${total.lines.covered} / ${total.lines.total} |`,
            `| Statements | ${total.statements.pct}% | ${total.statements.covered} / ${total.statements.total} |`,
            `| Functions | ${total.functions.pct}% | ${total.functions.covered} / ${total.functions.total} |`,
            `| Branches | ${total.branches.pct}% | ${total.branches.covered} / ${total.branches.total} |`,
            '',
          ];
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, `${lines.join('\n')}\n`);
          NODE

      - name: Upload desktop coverage artifact
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: desktop-coverage
          path: apps/desktop/coverage
          if-no-files-found: ignore

      - name: Build Vite frontend
        run: cd apps/desktop && bun run build:vite

      - name: Build Tauri app (debug, no bundle)
        timeout-minutes: 20
        run: cd apps/desktop && cargo tauri build --debug --no-bundle

  # AI Service tests
  ai-service:
    runs-on: ubuntu-latest
    name: AI Service
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: ".bun-version"

      - name: Cache Bun dependencies
        uses: actions/cache@9255dc7a253b0ccc959486e2bca901246202afeb # v5.0.1
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: TypeScript check
        run: cd apps/ai-service && bun run typecheck

      - name: Run unit tests
        run: bun run --filter ai-service test -- --coverage

      - name: AI Service coverage summary
        if: always()
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = 'apps/ai-service/coverage/coverage-summary.json';
          if (!fs.existsSync(path)) {
            console.log(`No coverage summary found at ${path}`);
            process.exit(0);
          }
          const total = JSON.parse(fs.readFileSync(path, 'utf8')).total;
          const lines = [
            '### AI Service Coverage',
            '',
            '| Metric | Percent | Covered / Total |',
            '| --- | ---: | ---: |',
            `| Lines | ${total.lines.pct}% | ${total.lines.covered} / ${total.lines.total} |`,
            `| Statements | ${total.statements.pct}% | ${total.statements.covered} / ${total.statements.total} |`,
            `| Functions | ${total.functions.pct}% | ${total.functions.covered} / ${total.functions.total} |`,
            `| Branches | ${total.branches.pct}% | ${total.branches.covered} / ${total.branches.total} |`,
            '',
          ];
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, `${lines.join('\n')}\n`);
          NODE

      - name: Upload AI Service coverage artifact
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ai-service-coverage
          path: apps/ai-service/coverage
          if-no-files-found: ignore

  e2e:
    runs-on: ubuntu-latest
    name: E2E (Web)
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: ".bun-version"

      - name: Cache Bun dependencies
        uses: actions/cache@9255dc7a253b0ccc959486e2bca901246202afeb # v5.0.1
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: Install Playwright browsers
        run: bunx playwright install --with-deps chromium

      - name: Run E2E tests
        run: bun run test:e2e

      - name: Upload Playwright HTML report
        if: failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload Playwright test results
        if: failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: playwright-test-results
          path: test-results
          if-no-files-found: ignore

  # Lint check
  lint:
    runs-on: ubuntu-latest
    name: Code Quality
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: ".bun-version"

      - name: Cache Bun dependencies
        uses: actions/cache@9255dc7a253b0ccc959486e2bca901246202afeb # v5.0.1
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: Performance audit
        run: bun run scripts/audit-performance.ts --fail-under 60

      - name: Check for lint errors (core)
        run: cd packages/core && bun run tsc --noEmit

      - name: Check for lint errors (desktop)
        run: cd apps/desktop && bun run lint

      - name: Check for lint errors (ai-service)
        run: cd apps/ai-service && bun run lint
